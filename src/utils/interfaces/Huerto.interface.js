

/**
 * 
 * @param {number} limit 
 * @param {number} page 
 * @param {string} name 
 */
export function IParamsHuertoList(
    limit,
    page,
    name
) {
    this.limit = limit
    this.page = page
    this.name = name
}